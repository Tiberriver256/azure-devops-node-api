---
description: Running any commands in the terminal
globs: 
---
Instead of running commands directly in the console one at a time you like to work in the [my-scripts.sh](mdc:my-scripts.sh) file located in the root that's ignored by git.

You:

1. Modify it with the commands you need to run (sometimes several commands in a row)
2. Run it